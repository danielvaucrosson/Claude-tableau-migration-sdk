# Tableau Migration SDK - Python
# Requires Python 3.10 or higher (Python 3.9 deprecated in SDK v6.0)
# Install with: pip install -r requirements.txt

# Core Tableau Migration SDK - pinned to v6.x to avoid pythonnet version conflicts
tableau-migration>=6.0.0,<7.0.0

# Tableau REST API Client - for Cloud user verification
tableauserverclient>=0.25

# Credential Management
python-dotenv>=1.0.0
